# 2026-07-20 — Seeders : renommage `demo_seeder.ts` → `malo_seeder.ts` et matrice plans/modules

`database/seeders/demo_seeder.ts` créait en réalité le compte réel de l'utilisateur admin (`ADMIN_EMAIL`), pas des données de démo génériques (celles-ci vivent dans `sandbox_seeder.ts`, "Marina Démo") — renommé en `database/seeders/malo_seeder.ts` (classe `MaloSeeder`), comportement inchangé.

Ajout de `database/seeders/billing_module_states_seeder.ts` (environnements `development`/`test`) qui matérialise chaque état de la matrice plan/abonnement/module de `/settings/billing` couverte par le fix #402 : Starter (upsell), Pro sans abonnement (admin vs membre), Pro abonné actif sans module (admin vs membre), Pro avec modules souscrits (`source: subscription`), Pro avec module offert (`source: granted`), Pro avec add-on `extra_boats` souscrit ou offert, Enterprise sans aucune ligne `organization_modules`, Enterprise avec lignes `granted` (mirroring du grandfathering). Comptes en `@test.local`, mot de passe `Password1!`, ne modifie pas les orgs de `test_plans_seeder.ts`.

Étendu avec 11 organisations supplémentaires : les 7 statuts d'abonnement Stripe (`trialing`, `past_due`, `canceled`, `incomplete`, `incomplete_expired`, `unpaid`, `paused` — seuls `active`/`trialing`/`past_due` sont considérés « actifs » par `SubscriptionService.getActive`), et 4 cas limites de quota Pro (stockage à 80% et au-delà de 100% des 20GB, tokens IA à 85% du quota mensuel, nombre de bateaux exactement au plafond de 8).

Complété avec les données métier des modules `charter`/`crm_invoicing` pour les 4 organisations où ils sont effectivement actifs (`pro-sub-with-modules`, `pro-granted-module`, `enterprise-baseline`, `enterprise-granted`) : tarification bateau + saison haute + réservations (confirmée/option) + contrat de location via `BoatPricingService`/`PricingSeasonService`/`BoatReservationService`/`RentalContractService`, et pour les orgs avec `crm_invoicing` en plus, 2 clients + un devis + une facture via `ClientService`/`InvoiceService` — `pro-granted-module` (charter seul) reste volontairement sans client ni facture pour vérifier le blocage de `/clients` et `/invoices`. Nouvelle doc `docs/dev/seeders.md` détaillant les 4 seeders (rôle, contenu, comptes créés).

Refactor de `billing_module_states_seeder.ts` (679 lignes) en fichiers de support : la classe `BaseSeeder` ne fait plus que 30 lignes et orchestre 4 fonctions (`seedCoreStates`, `seedSubscriptionStatuses`, `seedQuotaEdgeCases`, `seedModuleBusinessData`) déplacées dans `database/seeders_support/billing_module_states/` — un dossier frère de `database/seeders/`, jamais scanné par `db:seed` (contrainte `@adonisjs/lucid` : tout fichier `.ts` sous `database/seeders/` est traité comme un seeder). Comportement et données produites strictement inchangés (vérifié en base après refactor).
